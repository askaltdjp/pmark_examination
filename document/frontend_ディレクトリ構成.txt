/app
├── api                                // APIルート（app router 用）
│   ├── auth
│   │   └── login
│   │       └── route.ts               // 認証API（ログイン処理）
│   │   └── logout
│   │       └── route.ts               // 認証API（ログアウト処理）
│   └── exam
│        ├── start
│        │  └── route.ts               // 試験開始API
│        ├── answers
│        │  └── route.ts               // 試験回答保存APIpmark_examination_app
│        └── download
│            └── route.ts              // 試験結果ExcelダウンロードAPI
│
├── (pages)                            // 画面系サーバコンポーネント
│   ├── layout.tsx                     // 認証後レイアウト（Header/Footerあり）
│   ├── auth
│   │   └── login
│   │        ├── layout.tsx            // 認証前レイアウト（Header/Footerなし）
│   │        └── page.tsx              // ログイン画面
│   ├── home
│   │   └── dashboard
│   │        └── page.tsx              // ホーム画面（認証後）
│   ├── exam
│   │   ├── take
│   │   │   └── page.tsx               // 試験画面（問題表示・解答UI）（認証後）
│   │   └── result
│   │        └── page.tsx              // 試験結果画面（認証後）
│
├── middleware.ts                      // 認証ミドルウェア（/lib/middleware/* を呼び出す）

/components                            // クライアントコンポーネント群（UIパーツ）
├── common
│   ├── Header.tsx                     // 認証後レイアウト用の共通ヘッダー（ログアウトボタンあり）
│   ├── Footer.tsx                     // 認証後レイアウト用の共通フッター（必要に応じて）
│   ├── Button.tsx                     // 汎用ボタン（必要に応じて）
│   ├── Modal.tsx                      // 汎用モーダル（必要に応じて）
│   └── LoadingSpinner.tsx             // ローディングスピナーなど（必要に応じて）
├── auth
│   └── login
│        └── LoginForm.tsx             // ログインフォーム（必要に応じて）
├── home
│   └── dashboard
│        └── ...                       // ホーム画面用クライアントコンポーネント（必要に応じて）
├── exam
│   ├── take
│   │   ├── ExamQuestions.tsx          // 試験問題表示・回答UI（試験画面・試験結果画面で共通利用のUI）（必要に応じて）
│   │   └── QuestionNavigator.tsx      // 問題ナビゲーション（次へ、前へ）（試験画面・試験結果画面で共通利用のUI）（必要に応じて）
│   └── result
│        └── ExamResult.tsx            // 試験結果画面用のクライアントコンポーネント（必要に応じて）

/services                              // サービス層（ビジネスロジック）
├── api
│   ├── auth
│   │   ├── loginService.ts            // 認証API（ログイン処理）用サービス
│   │   └── logoutService.ts           // 認証API（ログアウト処理）用サービス
│   └── exam
│        ├── generateService.ts        // 試験問題生成API用サービス
│        ├── answersService.ts         // 試験回答保存API用サービス
│        └── downloadService.ts        // 試験結果ExcelダウンロードAPI用サービス
├── web
│   ├── auth
│   │   └── loginService.ts            // ログイン画面用サービス
│   ├── home
│   │   └── dashboardService.ts        // ホーム画面用サービス
│   └── exam
│        ├── takeService.ts            // 試験画面用サービス
│        └── resultService.ts          // 試験結果画面用サービス

/lib                                   // ライブラリ・外部接続・共通機能
├── prisma.ts                          // Prismaクライアント初期化
├── repositories                       // DBテーブル単位のリポジトリ層（リポジトリ名は仮）
│   ├── userRepository.ts
│   ├── examRepository.ts
│   ├── questionRepository.ts
│   ├── answerRepository.ts
│   ├── resultRepository.ts
│   └── ...（他のテーブルもここに追加）
├── utils                              // アプリ全体の共通ユーティリティ群
│   ├── authUtils.ts                   // 認証関連ユーティリティ（JWT生成・検証など）
│   ├── dateUtils.ts                   // 日付処理など（必要に応じて）
│   ├── stringUtils.ts                 // 文字列操作（必要に応じて）
│   ├── validationUtils.ts             // 入力バリデーション（必要に応じて）
│   └── index.ts                       // 各ユーティリティの再エクスポート用
└── middleware                         // 認証ミドルウェア関連の共通関数群
     └── authMiddleware.ts             // 認証用ミドルウェア関数

/templates                             // xlsxのテンプレートをまとめる
